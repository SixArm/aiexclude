##
# SixArm aiexclude file
#
# We use this aiexclude file as a starter file for our projects.
# We typically add many more items here depending on the project.
#
# https://github.com/sixarm/aiexclude/aiexlude
#
# These rules can be adjusted as you wish for your projects.
#
# We welcome constructive feedback, suggestions, questions, etc.
##

##
# Dot files and dot paths
##

# First reject dot path files, which are conventionally intended
# to be hidden files, or local only, or unversioned data, etc.
# Later on, you will override this rule to accept specific paths.
#
# Examples:
#
#     .foo
#     .foo.bar

.*

# Exclude dot paths that are considered encrypted paths.
# These paths are for secrets, confidential data, etc.
#
#     .foo.aes
#     .foo.age
#     .foo.argon2
#     .foo.bcrypt
#     .foo.gpg
#     .foo.pgp
#     .foo.scrypt

*.aes
*.age
*.argon2
*.bcrypt
*.gpg
*.pgp
*.scrypt

##
# Environment files and paths
##

# Exclude environment paths, which are conventionally intended
# to be configuration variables, or local only, or for secrets, etc.
#
#
# Examples:
#
#     .env
#     .env.foo
#     .envs
#     .envs.foo
#     env
#     env.foo
#     envs
#     envs.foo

.env
.env[-_.]*

.envs
.envs[-_.]*

env
env[-_.]*

envs
envs[-_.]*

##
# Artifacts that are typically omitted from version control
##

# Ignore backup directories and files.
#
# Examples:
#
#     backup
#     backups
#     backup.foo
#     backups.foo
#     foo.backup
#     foo.backups

backup
backups

*[-_.]*backup
*[-_.]*backups

backup[-_.]**
backups[-_.]**

# Ignore temporary directories and files.
#
# Examples:
#
#     tmp
#     temp
#     tmp.foo
#     temp.foo
#     foo.tmp
#     foo.temp

tmp
temp

*[-_.]*tmp
*[-_.]*temp

tmp[-_.]**
temp[-_.]**

# Ignore log directories and files.
#
# Examples:
#
#     log
#     logs
#     log.foo
#     logs.foo
#     foo.log
#     foo.logs
log
logs

*[-_.]*log
*[-_.]*logs

log[-_.]**
logs[-_.]**

# Ignore cache directories and files.
#
# Examples:
#
#     cache
#     caches
#     cache.foo
#     caches.foo
#     foo.cache
#     foo.caches

cache
caches

*[-_.]*cache
*[-_.]*caches

cache[-_.]**
caches[-_.]**
